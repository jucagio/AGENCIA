// Widget smoke test — placeholder until feature tests are implemented.
// TODO(Brook): Implement screen-level widget tests per feature.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // Tests will be added as features are implemented.
  // The default counter test has been removed — this app uses GoRouter + Riverpod.
  test('placeholder', () {
    expect(true, isTrue);
  });
}
