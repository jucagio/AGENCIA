# Ego Feedback — Hallazgo de seguridad CERRADO (2026-06-30, 11:48)

## Estado
🟢 **VERDE** — hallazgo crítico resuelto y verificado en vivo

## Cierre del hallazgo
Verificación post-fix contra la API REST pública de Supabase:

- `GET /rest/v1/accounts?select=password` → **`HTTP 401`**, `{"code":"42501","message":"permission denied for table accounts"}`. Confirmado: la columna `password` ya no es legible por `anon`.
- `GET /rest/v1/accounts?select=id,nombre,rol` → `HTTP 200`, `[]`. Confirmado: las columnas públicas siguen funcionando con normalidad para el flujo de login (lista de cuentas).

## Estado final del esquema Supabase
- Tablas creadas: `accounts`, `offers`, `stats`, `team_config`, `drafts` — todas con RLS habilitado y políticas activas.
- Funciones RPC operativas: `create_account`, `verify_login`, `reset_password` (security definer, bypass de RLS controlado).
- Columna `password`: protegida a nivel de privilegio de columna (no solo RLS) — no recuperable vía API pública bajo ninguna consulta.
- `supabase/schema.sql` en el repo actualizado y documentado con la causa raíz del bug de privilegios, para prevenir recurrencia en futuras tablas con datos sensibles.

## Recomendación
Ya es seguro que el usuario cree su cuenta real en `https://maper-offers.vercel.app` y proceda a validar el caso de uso original (login reconocido entre dispositivos).

## Nota sobre la falla del hook
El hook automático de Ego no pudo persistir los reportes anteriores de esta sesión por restricciones de permisos en modo "don't ask". Esta serie de reportes (10:50 → 11:38 → 11:43 → 11:48) documenta manualmente el ciclo completo: detección → diagnóstico → causa raíz → corrección → verificación → cierre.
