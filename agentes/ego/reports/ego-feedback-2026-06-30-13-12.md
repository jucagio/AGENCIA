# Ego Feedback — Email Auth setup (2026-06-30, 13:12)

## Estado
🟡 AMARILLO — proveedor Email activado en Supabase, pero el usuario reporta que la creación de cuenta sigue fallando. Investigando en curso.

## Avance
- Proveedor de Email confirmado activo (test directo a `/auth/v1/signup` ya no devuelve `email_provider_disabled`).
- UX de errores mejorada y desplegada: banner rojo persistente con el mensaje real de Supabase en vez de toasts genéricos.

## Pendiente
Usuario reporta que sigue sin poder crear cuenta tras activar el proveedor. Causa aún no identificada — siguiente paso es pedirle el mensaje exacto del banner de error o probar directamente contra la API con un correo real para aislar la causa.

## Nota sobre la falla del hook
Hook no pudo persistir el reporte por restricciones de permisos. Generado manualmente.
