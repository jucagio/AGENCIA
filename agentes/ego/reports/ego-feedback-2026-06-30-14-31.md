# Ego Feedback — Interruptor localizado, esperando confirmación (2026-06-30, 14:31)

## Estado
🟡 AMARILLO — causa raíz visualmente confirmada, falta que el usuario guarde el cambio

## Avance
El usuario compartió dos capturas de pantalla del dashboard de Supabase que permitieron diagnosticar con certeza visual (no solo por curl) la causa exacta del bloqueo:
- Captura 1: página "Sign In / Providers" mostraba la fila "Email" con badge "Disabled".
- Captura 2 (tras hacer clic en la fila): panel lateral "Email" con el interruptor **"Enable email provider"** apagado — confirmado como la causa raíz exacta de los errores `email_provider_disabled` detectados por curl en interacciones previas.

Se le indicó al usuario exactamente: activar "Enable email provider", dejar "Confirm email" desactivado (más abajo en el mismo panel, aún no visible en captura), no tocar el resto de toggles, y pulsar "Save".

## Pendiente
Esperando que el usuario confirme haber pulsado Save, para volver a verificar con curl contra `/auth/v1/signup` que el signup ya funciona de extremo a extremo.

## Nota sobre la falla del hook
Hook no pudo persistir el reporte por restricciones de permisos en modo "don't ask". Generado manualmente, consistente con el resto de la serie de esta sesión.
