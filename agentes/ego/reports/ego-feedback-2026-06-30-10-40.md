# Ego Feedback — Migración a Supabase (2026-06-30, 10:40)

## Estado
🟡 **AMARILLO** — implementación técnica completa, falta un paso manual del usuario.

## Resumen
La migración de "MAPER Offers" de `localStorage` a Supabase quedó implementada al 100% en código:

- `npm install @supabase/supabase-js` instalado.
- `src/lib/supabaseClient.js` creado (lee `VITE_SUPABASE_URL` / `VITE_SUPABASE_ANON_KEY` de env vars).
- `.env.local` creado y rellenado por el usuario con sus credenciales reales de Supabase.
- `.gitignore` actualizado (`node_modules`, `dist`, `.env*.local`).
- `supabase/schema.sql` escrito con: tablas `accounts`, `offers`, `stats`, `team_config`, `drafts`; RLS habilitado en las 5; columna `password` de `accounts` protegida (revoke select/insert/update/delete directo) y solo accesible vía 3 funciones RPC `security definer` (`create_account`, `verify_login`, `reset_password`) — las contraseñas nunca son legibles desde el navegador.
- `src/hub/MaperOffersModule.jsx` reescrito: login, crear cuenta, recuperar contraseña, autoguardado de ofertas, borrador/resume, stats, y gestión de equipo (RSM/Director) — todo migrado de `localStorage` a llamadas async a Supabase. Solo el puntero de sesión (`account_id` activo) sigue en `localStorage`, que es información no sensible.
- Build verificado limpio (`npm run build`) tras cada cambio.
- Variables de entorno `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY` configuradas en Vercel (production).
- Redeploy a producción ejecutado y verificado (`https://maper-offers.vercel.app` → redirige 307 a `https://maper-ofertas.vercel.app`, ambas responden 200).

## Bloqueador único
**El script SQL de `supabase/schema.sql` aún no ha sido ejecutado por el usuario en el SQL Editor de Supabase.** Sin las tablas creadas, cualquier intento de login/crear cuenta en producción fallará (la API de Supabase devolverá error de tabla/función inexistente). Se le entregó el SQL completo en el chat, listo para copiar/pegar, junto con instrucciones paso a paso (Supabase → SQL Editor → New query → Run).

## Próximo paso (bloqueado en espera del usuario)
1. Usuario corre el SQL en Supabase.
2. Usuario crea su cuenta de nuevo en `https://maper-offers.vercel.app` (esta sí queda persistida en la nube).
3. Validar el caso de uso original: iniciar sesión desde un segundo dispositivo y confirmar que reconoce la cuenta sin pedir crear usuario otra vez.
4. (Opcional, ya comunicado) evaluar si vale la pena migrar a Supabase Auth real para aislamiento criptográfico de datos entre cuentas — por ahora cualquier visitante con la anon key pública podría leer/escribir filas de `offers`/`team_config`/`stats` (mismo nivel de exposición que tenía localStorage, ahora centralizado).

## Hallazgos previos aún pendientes (sin cambios)
- Dependencias muertas en `package.json` (`express`, `cors`, `body-parser`, `recharts`, `react-hook-form`) y archivos huérfanos (`server.js`, `generador-interactivo.jsx`, `base_datos.json`, `scripts/`, `templates/`, `outputs/`).

## Nota sobre la falla del hook
El hook automático de Ego no pudo persistir este reporte por restricciones de permisos en modo "don't ask" (Write bloqueado para la herramienta del hook). Generado manualmente, consistente con los reportes anteriores de esta sesión.
