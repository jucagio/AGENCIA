# Ego Feedback — SMTP custom configurado pero falla envío (2026-06-30, 15:03)

## Estado
🟡 AMARILLO — rate limit de Supabase resuelto (usuario configuró Resend), pero el envío vía SMTP custom falla

## Avance
- Usuario creó cuenta en Resend y configuró Custom SMTP en Supabase (Project Settings → Authentication → SMTP Settings) siguiendo instrucciones: sender `onboarding@resend.dev`, host `smtp.resend.com`, puerto 465, usuario `resend`, password = API key de Resend.
- Verificación vía curl a `/auth/v1/signup`: `HTTP 500`, `{"error_code":"unexpected_failure","msg":"Error sending confirmation email"}`. El rate limit del proveedor gratuito de Supabase ya no es el problema (eso se confirma porque el error cambió de tipo) — ahora el SMTP custom está fallando al conectar/enviar.

## Hipótesis a verificar (pendiente captura de pantalla)
- Puerto incorrecto (465 vs 587 — Supabase puede esperar uno u otro según si es SSL implícito o STARTTLS).
- Username mal escrito (debe ser exactamente `resend`, minúsculas).
- API key con espacios extra al pegar, o key sin permisos de envío.
- Toggle "Enable Custom SMTP" no quedó realmente guardado/activo.

## Próximo paso
Esperando captura de pantalla del panel SMTP Settings de Supabase para comparar valores exactos contra lo solicitado a Resend, en vez de seguir iterando a ciegas.

## Nota sobre la falla del hook
Hook no pudo persistir el reporte por restricciones de permisos en modo "don't ask". Generado manualmente.
