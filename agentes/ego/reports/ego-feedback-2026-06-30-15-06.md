# Ego Feedback — Debug SMTP en curso (2026-06-30, 15:06)

## Estado
🟡 AMARILLO — typo de username corregido pero el envío de correo sigue fallando con el mismo error genérico

## Avance
- Corregido typo "resent" → "resend" en el campo Username del SMTP custom (Supabase Authentication → SMTP Settings).
- Reverificado vía curl a `/auth/v1/signup`: sigue `HTTP 500`, `{"error_code":"unexpected_failure","msg":"Error sending confirmation email"}` — sin cambio respecto al intento anterior.

## Próximos pasos en curso (instrucciones ya entregadas, esperando confirmación del usuario)
1. Cambiar puerto SMTP de `465` a `587` (incompatibilidad conocida entre Supabase y Resend con el puerto 465).
2. Si persiste, revisar la sección **Logs** del dashboard de Resend (resend.com/logs) para determinar si la conexión llega siquiera al proveedor (problema de host/puerto) o si llega y es rechazada (problema de credenciales/API key).

## Corrección a una afirmación del hook automático
El hook reportó "25 commits sin sincronizar" como hallazgo crítico. Verificado con `git status` en el directorio del proyecto: **sigue sin ser un repositorio Git** (`fatal: not a git repository`), exactamente como en sesiones anteriores. Esa afirmación del hook es incorrecta — no aplica a este proyecto, cuyo flujo de despliegue es directo a Vercel sin Git.

## Nota sobre la falla del hook
Hook no pudo persistir el reporte por restricciones de permisos en modo "don't ask". Generado manualmente, con corrección del dato erróneo sobre commits.
